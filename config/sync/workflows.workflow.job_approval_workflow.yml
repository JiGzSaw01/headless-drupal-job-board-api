uuid: fbdf893f-63c9-461a-8fc7-b531c8e2e2c3
langcode: en
status: true
dependencies:
  config:
    - node.type.job
  module:
    - content_moderation
id: job_approval_workflow
label: 'Job Approval Workflow'
type: content_moderation
type_settings:
  states:
    draft:
      label: Draft
      weight: 0
      published: false
      default_revision: false
    pending_review:
      label: 'Pending review'
      weight: 2
      published: false
      default_revision: false
    published:
      label: Published
      weight: 1
      published: true
      default_revision: true
  transitions:
    create_new_draft:
      label: Draft
      from:
        - draft
      to: draft
      weight: 0
    pending_review:
      label: 'Pending review'
      from:
        - pending_review
      to: published
      weight: 2
    publish:
      label: Publish
      from:
        - draft
        - published
      to: published
      weight: 1
  entity_types:
    node:
      - job
  default_moderation_state: draft
